language: python            # this works for Linux but is an error on macOS or Windows

services:
  - docker
  
before_install:
  - docker build -t 380app ./app/  
  - docker run -p 80:80 380app -d
  
install:
  - pip3 install -r requirements.txt

script:
  - pytest ./testing
